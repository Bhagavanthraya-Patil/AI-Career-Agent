# frontend/src/components

Reusable UI components organized by domain and complexity.

## Planned Components

- `Layout/` — App shell, navigation, sidebar
- `JobCard/` — Job listing preview card
- `ResumeViewer/` — Tailored resume preview
- `StatusBadge/` — Application status indicator
- `ScoreGauge/` — ATS match score visualization
- `FormFields/` — Reusable input, select, and button components
