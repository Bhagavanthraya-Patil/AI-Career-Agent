# frontend/public

Static files served directly by the web server without build processing.

## Contents

- `favicon.ico` — Browser tab icon
- `robots.txt` — Web crawler instructions
- `manifest.json` — PWA manifest (optional)
- `index.html` — HTML entry point (if not using Vite default)
