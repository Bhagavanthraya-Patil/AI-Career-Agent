# backend

FastAPI-based asynchronous backend service for the AI Career Agent. Handles API routing, business logic, database interactions, agent orchestration, and LLM gateway integration.

## Architecture

- `app/` — Main application package
- `tests/` — Pytest suite for backend

## Tech Stack

- Python 3.11+, FastAPI, SQLAlchemy, Alembic, Pydantic
