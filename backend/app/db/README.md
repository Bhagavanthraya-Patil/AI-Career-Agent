# backend/app/db

Database layer — models, migrations, repositories, and session management.

## Contents

- `models/` — SQLAlchemy ORM models (User, Job, Resume, Application)
- `migrations/` — Alembic migration scripts
- `repositories/` — Data access layer encapsulating query logic
- `session.py` — Database engine and session factory setup
