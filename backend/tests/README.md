# backend/tests

Pytest test suite for the backend application.

## Structure

- `unit/` — Unit tests for individual services, schemas, and utilities
- `integration/` — Integration tests for API endpoints and agent workflows
- `conftest.py` — Shared fixtures, mocks, and test configuration
