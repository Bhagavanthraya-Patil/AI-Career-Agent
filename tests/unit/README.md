# tests/unit

Unit tests for individual modules, services, schemas, and utilities.

## Scope

- Pydantic schema validation tests
- Service layer business logic tests
- Agent pure function tests
- Utility and helper function tests
- Repository/data access layer tests (with in-memory database)
