# tests/integration

Integration and end-to-end tests for cross-module workflows.

## Scope

- API endpoint integration tests (request → response validation)
- Agent orchestration workflow tests (multi-agent pipeline execution)
- Database migration tests
- LLM gateway integration tests (with mocked providers)
- Browser automation integration tests (with headless Playwright)
